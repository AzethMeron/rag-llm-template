[pytest]
pythonpath = src
testpaths = tests
# The test suite is self-contained: every model call is served by a scripted fake or an
# in-memory HTTP transport, so no inference server, GPU, or network is ever touched.
# See tests/README.md.
